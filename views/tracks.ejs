<% tracks.forEach(track => { %>
    <p><%- track.name %> </p>
    <%# track.external_urls.spotify %>
    <% if(track.preview_url === null) { %>
        <%# console.log("artistsName..........", track.preview_url) %> 
        <% return %>
        <% }%>
    <audio 
controls
src="<%- track.preview_url %>">
    <a href="<%- track.preview_url %>">
        Download audio
    </a>
</audio>
    
<% }) %>