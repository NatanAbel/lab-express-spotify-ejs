<% albums.forEach((album) => { %>

    <% console.log("ablumName.........", album.name) %> 
    <p><%- album.name%></p>
  <img src="<%= album.images[0].url%>" width="320" alt="picture" />
  <% console.log("album.artists", album.id) %> 
  <button><a href="/tracks/<%-album.id%> "> View Tracks </a></button>
<% }) %>